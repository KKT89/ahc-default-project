#pragma GCC optimize("Ofast")
#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

#include "lib/hp_params.hpp"
#include "lib/rnd32.hpp"
#include "lib/timer.hpp"

HP_PARAM(int, SAMPLE, 1, 0, 100);

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
}